version: '2'
services:
  web:
    image: 831650818513.dkr.ecr.us-east-1.amazonaws.com/xpeppers/aws-ecs-workshop:$TAG
    build:
      context: .
      args:
        TAG_VERSION_ARG: $TAG
    ports:
     - "80"
    cpu_shares: 400
    environment:
     - REDIS_HOST=123.cache.aws.log
    mem_limit: 134217728
